worker_processes 1;

events {
  worker_connections 1024;
}

http {
  server {
    listen 80;
    server_name localhost.fastapi.com;
    location / {
      proxy_pass http://fastapi_web:8000;
    }
  }
}
